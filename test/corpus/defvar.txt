================================================================================
defvar
================================================================================

(defvar x 10)
(defvar x)
(defvar x 10 "x is 10")

--------------------------------------------------------------------------------

(source
  (defvar (symbol) (number))
  (defvar (symbol))
  (defvar (symbol) (number) (documentation))
  )

================================================================================
defparameter
================================================================================

(defparameter x 10)
(defparameter x)
(defparameter x 10 "x is 10")

--------------------------------------------------------------------------------

(source
  (defparameter (symbol) (number))
  (defparameter (symbol))
  (defparameter (symbol) (number) (documentation))
  )

